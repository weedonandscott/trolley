name: Release

on:
  push:
    tags: ["v*"]
  pull_request:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (e.g. v0.1.0)"
        required: true

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.check.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
      - id: check
        run: |
          FILE_VERSION=$(cat VERSION | tr -d '[:space:]')
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            TAG="${{ inputs.tag || github.ref_name }}"
            VERSION="${TAG#v}"
            if [ "$VERSION" != "$FILE_VERSION" ]; then
              echo "::error::Tag $TAG does not match VERSION file ($FILE_VERSION)"
              exit 1
            fi
          fi
          echo "version=$FILE_VERSION" >> "$GITHUB_OUTPUT"

  test:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
      - name: Update Rust
        run: rustup update stable
      - name: Install just
        run: curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /usr/local/bin
      - name: Run unit tests
        run: just test

  build-linux:
    needs: validate
    strategy:
      matrix:
        include:
          - target: x86_64-linux
            runner: ubuntu-latest
          - target: aarch64-linux
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          submodules: recursive

      - uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: accept-flake-config = true

      - uses: cachix/cachix-action@v16
        with:
          name: trolley
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

      - name: Install just
        run: curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /usr/local/bin

      - name: Cache Zig deps
        id: deps
        run: |
          DEPS=$(just build-deps)
          echo "deps=$DEPS" >> "$GITHUB_OUTPUT"

      - name: Build and package
        run: nix develop -L -v -c just release --target ${{ matrix.target }} --system ${{ steps.deps.outputs.deps }}

      - name: Sanity test
        run: just sanity-test --target ${{ matrix.target }}

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-${{ matrix.target }}
          path: dist/trolley-*.tar.xz

  build-macos:
    needs: validate
    strategy:
      matrix:
        include:
          - target: x86_64-macos
            runner: macos-15-large
            # `nix develop` builds the bash it runs the command under from the
            # flake's own `nixpkgs` input, not from `nixpkgs-x86_64-darwin`.
            # nixos-unstable (26.11) throws for x86_64-darwin; nix ignores that
            # failure and falls back to Apple's bash 3.2, which cannot parse the
            # `;&` in stdenv's setup.sh. Override the input for this runner.
            nix_overrides: --override-input nixpkgs github:NixOS/nixpkgs/nixpkgs-26.05-darwin
          - target: aarch64-macos
            runner: macos-15
            nix_overrides: ""
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          submodules: recursive

      - uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: accept-flake-config = true

      - uses: cachix/cachix-action@v16
        with:
          name: trolley
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

      - name: Install tools
        run: |
          brew install just
          rustup update stable

      - name: Cache Zig deps
        id: deps
        run: |
          DEPS=$(just build-deps)
          echo "deps=$DEPS" >> "$GITHUB_OUTPUT"

      # Build the CLI outside the Nix shell so it links against macOS
      # system libraries instead of Nix store paths.
      - name: Build and package CLI
        run: just release-cli --target ${{ matrix.target }}

      - name: Build and package runtime
        run: nix develop ${{ matrix.nix_overrides }} -L -v -c just release-runtime --target ${{ matrix.target }} --system ${{ steps.deps.outputs.deps }}

      - name: Sanity test
        run: just sanity-test --target ${{ matrix.target }}

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-${{ matrix.target }}
          path: dist/trolley-*.tar.xz

  build-windows:
    needs: validate
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          submodules: recursive

      - name: Install Zig
        shell: pwsh
        run: |
          $fileContent = Get-Content -Path "runtime/build.zig.zon" -Raw
          $pattern = 'minimum_zig_version\s*=\s*"([^"]+)"'
          $zigVersion = [regex]::Match($fileContent, $pattern).Groups[1].Value
          $version = "zig-x86_64-windows-$zigVersion"
          Write-Output "Installing $version"
          $uri = "https://ziglang.org/download/$zigVersion/$version.zip"
          Invoke-WebRequest -Uri "$uri" -OutFile ".\zig-windows.zip"
          Expand-Archive -Path ".\zig-windows.zip" -DestinationPath ".\" -Force
          Remove-Item -Path ".\zig-windows.zip"
          Rename-Item -Path ".\$version" -NewName ".\zig"
          Write-Host "Zig installed:"
          .\zig\zig.exe version
          # Add zig to PATH for subsequent steps
          "$PWD\zig" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Setup environment
        shell: bash
        run: |
          rustup target add x86_64-pc-windows-gnu
          # MSYS2 provides cygpath (needed by just) and xz (needed by tar -J)
          echo "C:\msys64\usr\bin" >> "$GITHUB_PATH"
          cargo install just

      - name: Cache Zig dependencies
        uses: actions/cache@v4
        with:
          path: .zig-global-cache
          key: zig-cache-windows-${{ hashFiles('runtime/build.zig.zon', 'ghostty/build.zig.zon', 'ghostty/pkg/dcimgui/build.zig.zon') }}
          restore-keys: |
            zig-cache-windows-

      - name: Build and package
        shell: bash
        env:
          # Keep the global cache on the same drive as the checkout (D:\) to
          # work around ziglang/zig#25805 (assertion failure when cache and
          # source are on different drives, fixed after 0.15.2).
          ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-global-cache
        run: |
          # Zig's HTTP client on Windows is flaky on GitHub Actions runners.
          # Retry to handle transient connection failures during dependency fetching.
          for i in 1 2 3; do
            just release --target x86_64-windows && exit 0
            echo "::warning::Attempt $i/3 failed, retrying in 15s..."
            sleep 15
          done
          echo "::error::Build failed after 3 attempts"
          exit 1

      - name: Sanity test
        shell: bash
        run: just sanity-test --target x86_64-windows

      - uses: actions/upload-artifact@v4
        with:
          name: artifacts-x86_64-windows
          path: dist/trolley-*.tar.xz

  release:
    if: github.event_name != 'pull_request'
    needs: [validate, test, build-linux, build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}

      - uses: actions/download-artifact@v4
        with:
          pattern: artifacts-*
          merge-multiple: true

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          gh release create "v${VERSION}" \
            --title "trolley ${VERSION}" \
            --generate-notes \
            trolley-*.tar.xz

  update-homebrew:
    if: github.event_name != 'pull_request'
    needs: [validate, release]
    runs-on: ubuntu-latest
    steps:
      - name: Update Homebrew formula
        run: |
          git clone "https://x-access-token:${{ secrets.HOMEBREW_TAP_TOKEN }}@github.com/weedonandscott/homebrew-tap.git"
          cd homebrew-tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          ./update.sh "${{ needs.validate.outputs.version }}"
          git push
